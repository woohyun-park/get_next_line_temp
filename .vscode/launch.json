{
    "version": "0.2.0",
    "configurations": [

        {
            "type": "lldb",
            "request": "launch",
            "name": "Lldb debug",
            "program": "a.out",
            "args": [],
            "cwd": "${workspaceRoot}"
        }
    ]
}